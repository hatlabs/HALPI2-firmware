# HALPI2 Firmware Update Configuration

# Automatically flash firmware when firmware package is installed/upgraded
AUTO_FLASH_ON_INSTALL=yes

# Timeout for firmware flashing operation (seconds)
FLASH_TIMEOUT=30
